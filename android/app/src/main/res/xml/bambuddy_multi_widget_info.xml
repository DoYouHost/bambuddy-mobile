<?xml version="1.0" encoding="utf-8"?>
<!-- Multi-printer widget config: resizable, target 4x2. Updates are pushed by
     the app (updateWidget), so updatePeriodMillis=0. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/bambuddy_multi_widget"
    android:previewLayout="@layout/bambuddy_multi_widget"
    android:description="@string/widget_multi_description"
    android:updatePeriodMillis="0" />
